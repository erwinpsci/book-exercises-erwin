title = "Percentage of College Education vs Adult Poverty by State",
subtitle = subtitle,
x = "Percentage of College Education",
y = "Percentage of Adult Poverty",
color = "Urbanity" # legend label for color
)
View(state_race_long)
# 16.4 Building Map ----
# load the maps package
library(maps)
install.packages("maps") # install the maps package if not already installed
map_data("Canada")
map_data("canada")
canada_map <- map_data("world", region = "Canada") # map_data() is used to get the map data for a specific region, here we are getting the map data for Canada
# plot the map of canada
ggplot(data = canada_map) +
geom_polygon(mapping = aes(x = long, y = lat, group = group), fill = "lightblue", color = "black") + # geom_polygon() is used to create a polygon map, here we are using the `long` and `lat` columns for the x and y coordinates, and the `group` column to group the polygons
coord_fixed() + # coord_fixed() is used to fix the aspect ratio of the map
labs(title = "Map of Canada") # add a title to the map
# plot the map of British Columbia of Canada
bc_map <- map_data("world", region = "Canada:British Columbia") # get the map data for British Columbia
bc_map <- map_data("world", region = "Canada:British Columbia") # get the map data for British Columbia
bc_map <- map_data("world", region = "Canada::British Columbia") # get the map data for British Columbia
bc_map <- map_data("world", region = "Canada:British Columbia") # get the map data for British Columbia
# plot the map of British Columbia of Canada
# load a shapefile of US states using map_data function
state_shape <- map_data("state") # map_data() is used to get the map data for a specific region, here we are getting the map data for US states
state_shape <- map_data("state") # map_data() is used to get the map data for a specific region, here we are getting the map data for US states
# create a blank map of US states
ggplot(state_shape) +
geom_polygon(
mapping = aes(x = long, y = lat, group = group),
fill = "lightblue",
color = "black"
size = 0.1 # set the size of the polygon border
state_shape <- map_data("state") # map_data() is used to get the map data for a specific region, here we are getting the map data for US states
# create a blank map of US states
ggplot(state_shape) +
geom_polygon(
mapping = aes(x = long, y = lat, group = group),
fill = "lightblue",
color = "black",
size = 0.1 # set the size of the polygon border
) +
coord_map() # coord_map() is used to create a map projection, here we are using the default map projection
# then draw the same map for Canada provinces
canada_shape <- map_data("world", region = "Canada") # get the map data for Canada
ggplot() +
geom_polygon(
data = state_shape,
mapping = aes(x = long, y = lat, group = group),
fill = "lightblue",
color = "black",
size = 0.1 # set the size of the polygon border
) +
geom_polygon(
data = canada_shape,
mapping = aes(x = long, y = lat, group = group),
fill = "lightgreen",
color = "black",
size = 0.1 # set the size of the polygon border
) +
coord_map() + # coord_map() is used to create a map projection
labs(title = "Map of US States and Canada Provinces") # add a title to the map
ggplot(canada_shape) +
geom_polygon(
mapping = aes(x = long, y = lat, group = group),
fill = "lightblue",
color = "black",
size = 0.1 # set the size of the polygon border
) +
coord_map() + # coord_map() is used to create a map projection, here we are using the default map projection
labs(title = "Map of Canada Provinces") # add a title to the map
cities <- data.frame(
city = c("Vancouver", "Victoria", "Calgary", "Edmonton", "Winnipeg"),
lat = c(49.2827, 48.4284, 51.0447, 53.5461, 49.8951),
long = c(-123.1207, -123.3656, -114.0719, -113.4938, -97.1384)
)
# draw the state outlines, then plot the city point on the map
ggplot(canada_shape) +
geom_polygon(mapping = aes(x = long, y = lat, group = group), fill = "lightblue", color = "black", size = 0.1) +
geom_point(
data = cities,
mapping = aes(x = long, y = lat),
color = "red",
size = 3,
shape = 21, # shape 21 is a filled circle
fill = "yellow" # fill the circle with yellow color
) +
coord_map()
View(michigan_data)
setwd("~/OneDrive - University of Waterloo/book-exercises-erwin/chapter-16-exercises")
notices <- read.csv("Eviction_Notices", stringsAsFactors = FALSE) # read the eviction notices data, stringasFactors = FALSE means that the strings will not be converted to factors
notices <- read.csv("Eviction_Notices.csv", stringsAsFactors = FALSE) # read the eviction notices data, stringasFactors = FALSE means that the strings will not be converted to factors
View(notices)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(data, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(Location, c("lat", "long"), sep = ",") %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric("\\)", "", long) # convert the `long` column to numeric
)
View(notices)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(date, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(Location, c("lat", "long"), sep = ",") %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric("\\)", "", long) # convert the `long` column to numeric
)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(date, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(Location, c("lat", "long"), sep = ",") %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric(gsub("\\)", "", long)) # convert the `long` column to numeric
)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(date, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(location, c("lat", "long"), sep = ",") %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric(gsub("\\)", "", long)) # convert the `long` column to numeric
)
names(notices)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(date, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(Location, c("lat", "long"), sep = ",") %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric(gsub("\\)", "", long)) # convert the `long` column to numeric
)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(date, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(Location, c("lat", "long"), ",") %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric(gsub("\\)", "", long)) # convert the `long` column to numeric
)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(date, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(Location, c("lat", "long"), ",") %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric(gsub("\\)", "", long)) # convert the `long` column to numeric
)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>% # convert the `File.Date` column to a date format
filter(format(date, "%Y") == "2017") %>%  # filter the data to only include notices from 2017
separate(
Location,
c("lat", "long"),
","
) %>% # separate the `Location` column into `lat` and `long` columns, using a comma as the separator
mutate(
lat = as.numeric(gsub("\\(", "", lat)), # convert the `lat` column to numeric
long = as.numeric(gsub("\\)", "", long)) # convert the `long` column to numeric
)
colnames(notices)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>%
filter(format(date, "%Y") == "2017") %>%
mutate(
lat = as.numeric(lat),
long = as.numeric(long)
)
# create a mape of SF, with a point at teach eviction notice address
# use `install_github()` to install the newer version of `ggmap` on Github
# devtools: install_github("dkahle/ggmap")
library(ggmap)
library(ggplot2)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "toner-background", # maptype = "toner-background" means we are using the toner background map
darken = 0.7, # darken the map background by 50%
legend = "topleft" # legend position
)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "toner-lite", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "toner", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
# create a mape of SF, with a point at teach eviction notice address
# use `install_github()` to install the newer version of `ggmap` on Github
# devtools: install_github("dkahle/ggmap")
library(ggmap)
# create the background of the map titles
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_toner", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
notices <- notices %>%
filter(!is.na(lat), !is.na(long))
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
notices <- notices %>%
filter(!is.na(lat), !is.na(long))
nrow(notices)
View(notices)
View(notices)
# 16.5 mapping evictions in San Francisco ----
notices <- read.csv("Eviction_Notices.csv", stringsAsFactors = FALSE) # read the eviction notices data, stringasFactors = FALSE means that the strings will not be converted to factors
# data wrangling: format dates, filter to 2017 notices, extract lat/long data
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>%
filter(format(date, "%Y") == "2017") %>%
mutate(
lat = as.numeric(lat),
long = as.numeric(long)
)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>%
filter(format(date, "%Y") == "2017") %>%
mutate(
lat = as.numeric(lat),
long = as.numeric(long)
)
notices <- read.csv("Eviction_Notices.csv", stringsAsFactors = FALSE)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>%
filter(format(date, "%Y") == "2017") %>%
separate(Location, c("lat", "long"), ", ") %>%  # this splits the 'Location' column
mutate(
lat = as.numeric(gsub("\\(", "", lat)),       # remove '('
long = as.numeric(gsub("\\)", "", long))      # remove ')'
)
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>%
filter(format(date, "%Y") == "2017") %>%
separate(Location, c("lat", "long"), ", ") %>%  # this splits the 'Location' column
mutate(
lat = as.numeric(gsub("\\(", "", lat)),       # remove '('
long = as.numeric(gsub("\\)", "", long))      # remove ')'
) %>%
filter(!is.na(lat), !is.na(long))  # remove malformed or incomplete data
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
# 16.5 mapping evictions in San Francisco ----
notices <- read.csv("Eviction_Notices.csv", stringsAsFactors = FALSE) # read the eviction notices data, stringasFactors = FALSE means that the strings will not be converted to factors
# data wrangling: format dates, filter to 2017 notices, extract lat/long data
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>%
filter(format(date, "%Y") == "2017") %>%
separate(Location, c("lat", "long"), ", ") %>%  # this splits the 'Location' column
mutate(
lat = as.numeric(gsub("\\(", "", lat)),       # remove '('
long = as.numeric(gsub("\\)", "", long))      # remove ')'
)
notices <- read.csv("Eviction_Notices.csv", stringsAsFactors = FALSE) # read the eviction notices data, stringasFactors = FALSE means that the strings will not be converted to factors
# data wrangling: format dates, filter to 2017 notices, extract lat/long data
notices <- notices %>%
mutate(date = as.Date(File.Date, format = "%m/%d/%Y")) %>%
filter(format(date, "%Y") == "2017") %>%
separate(Location, c("lat", "long"), ", ") %>%  # this splits the 'Location' column
mutate(
lat = as.numeric(gsub("\\(", "", lat)),       # remove '('
long = as.numeric(gsub("\\)", "", long))      # remove ')'
)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
register_stadiamaps(key = "1d468775-ba8a-41a7-91bf-60a49f383d7a")
register_stadiamaps(key = "1d468775-ba8a-41a7-91bf-60a49f383d7a", write = TRUE)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
register_stadiamaps(key = "1d468775-ba8a-41a7-91bf-60a49f383d7a")
# create the background of the map titles
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
base_plot <- qmplot(
data = notices,
x = long,
y = lat, # longitude and latitude coordinates
geom = "blank", # geom = "blank" means we are creating a blank map
maptype = "stamen_terrain", # maptype = "toner-lite" means we are using the toner-lite map type
darken = 0.5, # darken the map by 50%
legend = "topleft" # no legend is needed
)
# add the location of evictions to the map
base_plot +
geom_point(
data = notices,
mapping = aes(x = long, y = lat),
color = "red",
size = 0.5,
alpha = 0.5 # set transparency to 50%
) +
labs(title = "Eviction Notices in San Francisco (2017)", x = "Longitude", y = "Latitude") + # add title and axis labels
theme_minimal() # use a minimal theme for the plot
source("api_keys.R")
register_stadiamaps(key = "1d468775-ba8a-41a7-91bf-60a49f383d7a")
touch .gitignore
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = stat(level)),
alpha = 0.3
) +
scale_fill_gradient(
"# of Evictions", # label for the fill scale
low = "blue",
mid = "yellow",
high = "red") + # set the color gradient from blue to red
labs(title = "Eviction Notices in San Francisco (2017)", x = "Longitude", y = "Latitude") + # add title and axis labels
theme_minimal(plot.margin = margin(0.3, 0, 0, 0, "cm")) # use a minimal theme for the plot
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = stat(level)),
alpha = 0.3
) +
scale_fill_gradient(
"# of Evictions", # label for the fill scale
low = "white",
mid = "yellow",
high = "red"
) + # set the color gradient from blue to red
labs(title = "Eviction Notices in San Francisco (2017)", x = "Longitude", y = "Latitude") + # add title and axis labels
theme_minimal(plot.margin = margin(0.3, 0, 0, 0, "cm")) # use a minimal theme for the plot
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = stat(level)),
alpha = 0.3
) +
scale_fill_gradient2(
"# of Evictions", # label for the fill scale
low = "white",
mid = "yellow",
high = "red"
) + # set the color gradient from blue to red
labs(title = "Eviction Notices in San Francisco (2017)", x = "Longitude", y = "Latitude") + # add title and axis labels
theme_minimal(plot.margin = margin(0.3, 0, 0, 0, "cm")) # use a minimal theme for the plot
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = stat(level)),
alpha = 0.3
) +
scale_fill_gradient2(
"# of Evictions", # label for the fill scale
low = "white",
mid = "yellow",
high = "red"
) + # set the color gradient from blue to red
labs(title = "Eviction Notices in San Francisco (2017)", x = "Longitude", y = "Latitude") + # add title and axis labels
theme(plot.margin = margin(0.3, 0, 0, 0, "cm")) # use a minimal theme for the plot
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = after_stat(level)),
alpha = 0.3
) +
scale_fill_gradient2(
"# of Evictions",
low = "white",
mid = "yellow",
high = "red"
) +
labs(
title = "Eviction Notices in San Francisco (2017)",
x = "Longitude",
y = "Latitude"
) +
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = after_stat(level)),
alpha = 0.3
) +
scale_fill_gradient2(
"# of Evictions",
low = "white",
mid = "yellow",
high = "red"
) +
labs(
title = "Eviction Notices in San Francisco (2017)",
x = "Longitude",
y = "Latitude"
) +
theme(plot.margin = margin(0.3, 0, 0, 0, "cm"))
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = after_stat(level)),
alpha = 0.3
) +
scale_fill_gradient2(
"# of Evictions",
low = "white",
mid = "yellow",
high = "red"
) +
labs(
title = "Eviction Notices in San Francisco (2017)",
x = "Longitude",
y = "Latitude"
) +
theme(plot.margin = margin(0.3, 0, 0, 0, "cm"))
base_plot +
geom_polygon(
stat = "density2d",
mapping = aes(fill = after_stat(level)),
alpha = .3
) +
scale_fill_gradient2(
"# of Evictions",
low = "white",
mid = "yellow",
high = "red"
) +
labs(title = "Number of Evictions in San Francisco, 2017") +
theme(plot.margin = margin(.3, 0, 0, 0, "cm"))
base_plot +
stat_density2d(
geom = "polygon",
mapping = aes(x = long, y = lat, fill = after_stat(level)),
alpha = 0.3
) +
scale_fill_gradient2(
"# of Evictions",
low = "white",
mid = "yellow",
high = "red"
) +
labs(
title = "Number of Evictions in San Francisco, 2017",
x = "Longitude",
y = "Latitude"
) +
theme(plot.margin = margin(0.3, 0, 0, 0, "cm"))
